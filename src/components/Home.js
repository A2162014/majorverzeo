import React from 'react';

const Home = () => {
  return (
    <div className="animate">
      <h1>Welcome to the ReactJS Major Project</h1>
      <p>This is a single-page application built with React, Bootstrap, and CSS.</p>
    </div>
  );
};

export default Home;
